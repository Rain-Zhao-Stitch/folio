{
  "name": "folio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node --check dist/app.js && node --check dist/translation.js && node --check server.cjs",
    "build": "esbuild src/curl.js --bundle --format=iife --minify --outfile=dist/curl.js",
    "verify": "node scripts/verify.cjs",
    "start": "node server.cjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "epubjs": "^0.3.93",
    "jszip": "^3.10.2",
    "three": "^0.186.0"
  },
  "overrides": {
    "@xmldom/xmldom": "^0.9.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "esbuild": "^0.28.2",
    "pngjs": "^7.0.0"
  }
}
