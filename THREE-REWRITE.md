# Three.js 重写说明

## 交付范围

翻页渲染已从 StPageFlip / CSS 切片重写为 Three.js WebGL2 曲面。
方向键、按钮、双指横滑和边缘拖动共用一个渲染器；方向键与触控板使用书脊卷页，
鼠标边缘拖动使用单独的上/下页角卷曲；触控板距离控制进度，
超过中线后手势结束则翻页，否则回弹。保留 EPUB.js 排版、本地 IndexedDB
书库、进度、字体、深色模式和离线翻译，原网址及存储结构不变。

选用网页宿主，复用现有排版与浏览器 GPU，不额外引入 Electron。
这不构成跨设备性能优于所有原生实现的保证。

## 实现与修复

- 顶点着色器使用随手势移动的局部圆柱折线。鼠标拖角时，折痕取原始页角与当前指针之间的垂直平分线，因此始终与拉扯方向垂直；纸张越过圆弧后沿切线继续延伸并保持抬起，不会在动画中变成与底页平行的悬浮平面。键盘和触控板仍保持与书脊平行的卷页。
- WebGL2 画布显式禁用 alpha，纸面禁用颜色混合并写入深度缓冲，避免底层正文透出。
- 静止时使用 CSS 书脊；翻页时在 WebGL 中绘制固定书脊并参与深度测试，因此空出的书脊保持可见，卷页覆盖处则被真实遮挡，不会投影到曲面或被切成虚线。
- 鼠标页角形变在 0%/100% 与完整纸张严格重合，只在拖动中段产生角点延迟，避免结束时卡在半页后跳变。
- 窗口失焦或页面隐藏会立即取消未完成的鼠标、触控板手势，防止等待释放事件时永久卡住。
- 鼠标按下位置作为卷页零点，GPU 进度以短时逐帧插值追随稀疏的 pointer 事件，松手后从屏幕上的实际进度继续收页。
- 鼠标在页边任意位置抓取时都使用实际抓取点作为纸张锚点；折痕取锚点与指针的垂直平分线，因此中段与页角遵循完全相同的法线规则。
- GPU 曲面到静态 EPUB DOM 使用 64ms 透明交接，且先恢复实时正文再释放 WebGL 画布，避免合成层销毁时闪出纯色帧。
- 翻页期间保留 EPUB 正文作为不透明 WebGL 层下方的兜底，避免共享画布从离屏缓存移入前台时因合成延迟闪出纯色背景。
- 深色纯黑纸面不绘制投影高光；浅色纸面只保留柔和的黑色落影。
- 单个 WebGL 上下文；缓存时上传纹理、编译着色器；进度不变时不重复绘制。
- 翻页落定后立即预热同方向的下一张曲面，期间累积连续触控板输入并在就绪后播放完整卷页；冷启动不再绘制黑色侧边或降低正文亮度。
- 释放失效缓存的纹理、几何体与材质，避免连续翻页累积 GPU 资源。
- SVG foreignObject 快照经自包含 data URL 转换为可上传纹理，避免 Blob SVG 污染 Canvas。
- 保留原正文宽度，防止单页快照重新分栏；正反向 UV 分别映射，避免背面文字镜像。
- GPU 缺失时保留普通翻页；上下文丢失时清理当前动画，恢复后可继续使用 GPU。
- 新动画入口统一处理边缘手势的宽度与回弹判定，避免迁移时出现 NaN 进度。

## 验证

执行 `npm run build`、`npm test`、`npm run verify`。
统一验证入口运行 48 个脚本，必要时启动自己的本地服务，结束时只关闭自己启动的服务。
新增 GPU 渲染、上下文恢复、无 WebGL 降级测试；迁移旧动画 DOM 断言，补充
画面起止像素对比、25% / 50% / 75% 圆柱卷页阶段、书脊层级、按住不动不重绘、正反向导航和实际 GPU 资源计数断言。
覆盖普通 / 高 DPI、双页 / 窄屏单页。截图位于 test-results。

同机无头 Edge 长章节样例观测：

| 指标 | 旧版观测 | Three.js 观测 |
|---|---:|---:|
| 动画帧间隔 P95 | 约 8.8 ms | 约 3.3 ms |
| 最大动画帧间隔 | 约 41.8 ms | 约 9.5 ms |
| 缓存首帧响应 | 约 21.7 ms | 约 12.1–13.6 ms |

以上是自动化样例结果，不是实体屏幕帧率保证。冷启动仍需进行 EPUB 排版和纹理准备，
此时先显示侧边反馈。动画中文字使用纹理灰度抗锯齿，与静态 DOM 的 LCD 抗锯齿略有差异。

## 尚需实体环境验证

- JoyXoff 全屏自动停用是外部程序配置问题；此重写不宣称已经恢复 Windows 系统光标。
  需要在 JoyXoff 对应浏览器配置中核对“全屏时不禁用”，并用实际手柄复验。
- 未声称所有 EPUB 均兼容：DRM、损坏文件、复杂固定版式及交互书籍仍有限制。
- 未配置独立 lint / TypeScript 检查；现有 JavaScript 语法检查与打包检查已纳入命令。
- 不执行自动休眠。
